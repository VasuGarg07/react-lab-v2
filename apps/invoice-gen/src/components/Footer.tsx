export const Footer = () => (
    <footer className="border-t border-hairline">
        <div className="mx-auto flex max-w-7xl flex-col items-center justify-between gap-2 px-4 py-5 text-xs text-faint sm:flex-row sm:px-6">
            <span>
                Built by <span className="font-medium text-muted">Vasu Garg</span> · PDF rendered server-side with ReportLab
            </span>
            <span>Invoice Studio</span>
        </div>
    </footer>
);
